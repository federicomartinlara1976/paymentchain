spring:
  application:
    name: product
    version: 1.0.0
    
  config.import: "optional:configserver:"
  cloud:
    config:
      name: product
      enabled: true
      uri: http://192.168.1.135:8888
