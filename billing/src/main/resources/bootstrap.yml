spring:
  application:
    name: billing
    
  profiles:
    active: ${ENV}
  
  config.import: "optional:configserver:"
  cloud:
    config:
      name: billing
      enabled: true
      uri: http://192.168.1.135:8888
